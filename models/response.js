/**
 * Created by max on 8/5/14.
 */

var model = require('nodejs-model');

var Response = model("Response")
        .attr('token',{

        })
        .attr('result', {

        })
        .attr('message', {

        })
        .attr('error', {

        })
        .attr('data', {

        })
    ;